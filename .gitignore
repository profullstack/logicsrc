node_modules
dist
.env
.env.*
!.env.example
.DS_Store
coverage
playwright-report
test-results
*.tsbuildinfo
.commandboard
.logicsrc
supabase/.temp

# Nixpacks build plan generated by `railway up`. It bakes absolute paths to the
# worktree it was generated in and a one-off image tag, so it is never reusable
# and every regeneration conflicts with the last one.
